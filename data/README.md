# Data Directory

Place local ESA CDM CSV datasets here.

Expected filenames for the documented workflow:
- `data/train_data.csv`
- `data/test_data.csv`

These files are ignored by git because they are local experiment inputs.
